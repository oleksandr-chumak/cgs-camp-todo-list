import React from 'react';
import * as Styled from './button-pagination.styled';

export const PaginationGapItem = () => (
  <Styled.PaginationItemGapWrapper>
    <Styled.PaginationGapIcon />
  </Styled.PaginationItemGapWrapper>
);
